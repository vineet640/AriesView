{
  "name": "ariesview-api-gateway",
  "version": "1.0.0",
  "description": "AriesView API gateway: JWT auth, hybrid retrieval, dedup, prompt assembly, LLM generation",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "NODE_OPTIONS=--localstorage-file=/tmp/ariesview-jest-localstorage jest --runInBand"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
